<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Profile</title>
</head>
<body>
    <h3>Institute : <%= student.institute ? student.institute.iname : "N/A" %></h3>
    <h3>Name : <%= student.fullname %> </h3>
    <h3>Hall Ticket No. : <%= student.htnum %> </h3>
    <h3>Email : <%= student.email %> </h3>
    <h3>Year : <%= student.year ? student.year.year : "N/A" %></h3>
    <h3>Department : <%= student.department ? student.department.name : "N/A" %></h3>
    <h3>Division : <%= student.division ? student.division.divisionName : "N/A" %></h3>
    
    <h3>Subjects:</h3>
<ul>
  <% if (student.subject && student.subject.length > 0) { %>
    <% student.subject.forEach(subject => { %>
      <li>
        <strong><%= subject.subjectName && subject.subjectName.name ? subject.subjectName.name : "N/A" %></strong>
        <ul>
          <li>Code: <%= subject.subjectName && subject.subjectName.code ? subject.subjectName.code : "N/A" %></li>
          <li>CIE Submission: <%= subject.submissionStatus && subject.submissionStatus.CIE ? subject.submissionStatus.CIE : "N/A" %></li>
          <li>TA Submission: <%= subject.submissionStatus && subject.submissionStatus.TA ? subject.submissionStatus.TA : "N/A" %></li>
        </ul>
      </li>
    <% }) %>
  <% } else { %>
    <p>No subjects assigned yet.</p>
  <% } %>
</ul>
 
    
</body>
</html>
